export const metadata = {
  title: 'NFL Prop Builder',
  description: 'NFL prop research and parlay builder',
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body
        style={{
          fontFamily: 'system-ui, -apple-system, sans-serif',
          background: '#0b0d12',
          color: '#f5f5f5',
          margin: 0,
          minHeight: '100vh',
        }}
      >
        {children}
      </body>
    </html>
  );
}
